class Solution {
    public boolean stoneGame(int[] piles) {
       return true;
       /// logic recursion and dp works pick and pick not
       ///prefix and suffix array also work like taking 1 from preffix and rest from suffix or vice versa
       // subarray approach also works like take half of choices and maximum it and minimuse the other half.. 
    }
}